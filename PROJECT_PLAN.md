# F&B 매출 관리 대시보드 구축 기획안 (최종 v1.1)

## 1. 프로젝트 개요

### 1.1 배경 및 목적
본 프로젝트는 F&B 매장에서 발생하는 POS 매출 데이터와 영업 외 수기 매출을 통합 관리하기 위한 웹 기반 대시보드 시스템입니다. 복잡한 엑셀 데이터를 자동으로 정제하여 데이터베이스화하고, 이를 시각화하여 매장 운영자가 직관적으로 매출 현황을 파악하고 의사결정을 내릴 수 있도록 돕는 것을 목적으로 합니다.

### 1.2 주요 목표
- **데이터 통합**: POS 엑셀 파일과 수기 입력 데이터를 단일 데이터베이스로 통합
- **자동화된 정제**: 원본 데이터의 노이즈(진동벨, 0원 옵션 등)를 자동으로 제거하고 유효한 데이터만 추출
- **시각화**: 시간대별/일별 추이, 상품별 매출 비중, 인기 메뉴 등을 차트로 시각화
- **운영 효율화**: 매일 매출 리포트를 Slack으로 자동 발송하여 업무 부하 감소

### 1.3 기술 스택
- **Frontend**: Next.js 14 (App Router), TypeScript, Tailwind CSS, Shadcn UI
- **Backend**: Next.js Server Actions, Vercel Serverless Functions
- **Database**: Supabase (PostgreSQL)
- **Visualization**: Recharts
- **Deployment**: Vercel

---

## 2. 시스템 아키텍처 및 데이터 흐름

### 2.1 시스템 구조
1.  **Client (Web Browser)**: 사용자는 웹 인터페이스를 통해 데이터를 업로드하거나 조회합니다.
2.  **Server (Next.js)**: 업로드된 엑셀 파일을 파싱하고 검증 로직을 수행한 후 DB와 통신합니다.
3.  **Database (Supabase)**: 정제된 매출 데이터와 요약 통계를 저장합니다.
4.  **External (Slack)**: 정해진 스케줄에 따라 일일 리포트 알림을 수신합니다.

### 2.2 데이터 처리 파이프라인
`Raw Excel Upload` -> `Parsing (xlsx)` -> `Filtering & Validation` -> `Data Cleansing (Delete old data)` -> `DB Upsert` -> `Dashboard Rendering`

---

## 3. 데이터베이스 설계

### 3.1 테이블 구조
| 테이블명 | 설명 | 주요 컬럼 |
| :--- | :--- | :--- |
| **sales_records** | 개별 상품 판매 내역 | `id`, `sale_date`, `payment_time`, `receipt_number`, `pos_number`, `category`(Nullable), `item_name`, `quantity`, `total_amount`, `discount_amount`, `net_amount`, `is_refund`, `source` |
| **daily_summary** | 일별 매출 요약 | `sale_date`, `total_sales`, `pos_sales`, `manual_sales` |
| **manual_inputs** | 영업 외 수기 입력 내역 | `sale_date`, `description`, `amount`, `created_at` |

### 3.2 데이터 무결성 전략
- **중복 방지**: 데이터 업로드 시, 해당 날짜의 기존 POS 데이터를 **삭제 후 재입력**하여 중복 방지 및 최신 데이터 유지
- **집계 정확성**: `daily_summary`는 기존 값 누적이 아닌, 현재 DB의 전체 레코드를 **재집계**하여 업데이트 (UPSERT)

---

## 4. 상세 기능 명세

### 4.1 데이터 입력 및 관리
- **POS 데이터 업로드**:
    - `.xlsx`, `.xls` 파일 지원
    - **입력 데이터 필드**: 포스번호, 영수증번호, 구분(매출/반품), 결제시각, 상품명, 수량, 총매출액, 할인액, 실매출액 등
    - **지능형 필터링 및 데이터 정제**:
        - **기본 검증**: 상품명이 없거나 공백인 데이터 제외 (`null`/`empty` 체크)
        - **반품 처리**: '반품' 구분 데이터는 `is_refund=true`로 저장하고 매출 집계 시 차감
        - **운영 카테고리 제외**: '커피벨', '브런치벨' 등 매출과 무관한 운영 데이터 자동 제외
        - **옵션 상품 조건부 제외**: '샷추가', '아이스' 등의 옵션 키워드가 포함되어 있어도 **실매출액이 0원인 경우에만 제외** (유료 옵션은 보존)
- **영업 외 매출 입력**:
    - POS에 잡히지 않는 추가 수익 수기 입력 기능
    - 날짜, 항목, 금액 입력 시 실시간 합산 반영

### 4.2 대시보드 시각화
- **KPI Cards**: 오늘 매출, 전일 대비 증감률, 이번 달 누적 매출
- **Hourly Sales (Line Chart)**: **[신규]** 오늘 시간대별 매출 흐름 시각화
- **Daily Sales Trend (Line Chart)**: 최근 30일간의 일별 매출 추이 그래프
- **Product Share (Pie Chart)**: **[개선]** 상품별 매출 비중 분석 (기타 항목 제외, 상위 10개 표시)
- **Best Menu Ranking**: 판매량 기준 상위 5개 메뉴 리스트

### 4.3 누적 데이터 조회 (Data View)
- **기간별 조회**: Date Range Picker를 통해 원하는 기간의 데이터를 필터링하여 조회
- **성능 최적화**: 서버 사이드 필터링 및 데이터 제한(Limit)을 통해 대량 데이터 조회 시 성능 저하 방지
- **상세 정보 표시**: 영수증 번호, 결제 시각, 반품 여부 등 상세 정보 테이블 제공

### 4.4 시스템 설정 및 자동화
- **테마 설정**: 시스템/라이트/다크 모드 지원
- **Slack 알림**: 매일 오전 9시, 전일 매출 요약 리포트 자동 발송 (Vercel Cron Jobs)

---

## 5. 개발 완료 사항 및 품질 개선 리포트

### 5.1 주요 해결 이슈
1.  **데이터 구조 변경 대응**:
    - *문제*: POS 데이터 포맷 변경(대분류 삭제, 영수증번호/결제시각 추가)으로 인한 파싱 오류
    - *해결*: DB 스키마 업데이트 및 파싱 로직 전면 개편

2.  **데이터 무결성 강화**:
    - *문제*: 파일 재업로드 시 데이터 중복 및 반품 데이터 처리 미비
    - *해결*: '삭제 후 재입력' 로직 도입 및 반품 데이터(`is_refund`) 식별 처리 추가

3.  **대시보드 가시성 개선**:
    - *문제*: 카테고리 정보 부재로 인한 차트 공백 및 '기타' 항목 과다 표시
    - *해결*: 카테고리 차트 제거 -> '시간대별 차트' 신설, 상품 차트에서 '기타' 제외 및 Top 10 위주 표시

### 5.2 산출물 목록
- **소스 코드**: Next.js 14 프로젝트 전체
- **문서**:
    - `ARCHITECTURE.md`: 시스템 상세 아키텍처 문서
    - `SETUP_GUIDE.md`: 개발 환경 셋업 및 배포 가이드
    - `PROJECT_PLAN.md`: 본 기획안 문서
- **데이터베이스 스크립트**: `schema.sql`, `update_schema.sql` (마이그레이션)

---

## 6. 향후 발전 계획

- **기간별 비교 분석**: 작년 동월 대비(YoY), 전월 대비(MoM) 상세 분석 기능
- **사용자 관리**: 매장 매니저 및 직원 등 권한별 접근 제어 (Auth)
- **재고 연동**: 메뉴 판매량에 따른 예상 재료 소진량 계산
